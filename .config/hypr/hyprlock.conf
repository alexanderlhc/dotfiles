general {
    hide_cursor = true
}

background {
    monitor =
    color = rgba(20, 20, 28, 1.0)
    blur_passes = 2
}

input-field {
    monitor =
    size = 300, 50
    outline_thickness = 2
    dots_size = 0.25
    fade_on_empty = true
    placeholder_text = <i>Password…</i>
    position = 0, -80
    halign = center
    valign = center
}

label {
    monitor =
    text = $TIME
    font_size = 64
    position = 0, 80
    halign = center
    valign = center
}
