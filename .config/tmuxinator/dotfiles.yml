# /home/alexander/.config/tmuxinator/dotfiles.yml

name: dotfiles
root: ~/.config/

windows:
  - editor:
      layout: main-vertical
      panes:
        - dotgit
  - neovim:
      root: ~/.config/nvim
      panes: nvim
  - tmux:
      root: ~/.config/tmux
      panes: nvim
  - tmuxinator:
      root: ~/.config/tmuxinator
      panes: nvim
  - hypr:
      root: ~/.config/hypr
      panes: nvim
  - ags:
      root: ~/.config/ags
      panes: nvim
  - zsh:
      root: ~/.config/zsh
      panes: nvim
