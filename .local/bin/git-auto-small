#!/bin/bash

# Generate a unique branch name with a timestamp
branch_name="auto-small-$(date +%Y%m%d%H%M%S)"

# Create a new branch, add all changes, and commit
git checkout -b "$branch_name" && git add . && git commit -m "Auto commit: small things"
