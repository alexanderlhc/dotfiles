{
  "layer": "top",
  "position": "top",
  "modules-left": ["hyprland/workspaces", "sway/workspaces"],
  "modules-center": [],
  "modules-right": ["pulseaudio", "tray", "battery", "clock", "custom/power"],

  "pulseaudio": {
    "format": " {volume}%",
    "format-muted": " muted",
    "on-click": "pkill pavucontrol || pavucontrol"
  },

  "clock": {
    "format": "{:%Y-%m-%d %H:%M}"
  },

  "tray": {
    "icon-size": 18,
    "spacing": 8
  },

  "battery": {
    "states": {
      "warning": 30,
      "critical": 15
    },
    "format": "{icon} {capacity}%",
    "format-charging": " {capacity}%",
    "format-plugged": " {capacity}%",
    "format-alt": "{time} {icon}",
    "format-icons": ["", "", "", "", ""],
    "tooltip-format": "{timeTo}"
  },

  "sway/workspaces": {
    "disable-scroll": true,
    "all-outputs": true
  },

  "custom/power": {
    "format": "⏻",
    "on-click": "~/.config/sway/power-menu.sh"
  }
}
